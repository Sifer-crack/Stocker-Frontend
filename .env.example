# Gateway address for local `npm run dev` / `npm run build` (see Stocker-Backend `gateway`).
# Defaults to http://localhost:8080 if unset.
# In Docker this is NOT used: pass the runtime env var API_BASE_URL to the container instead.
VITE_API_BASE_URL=http://localhost:8080
